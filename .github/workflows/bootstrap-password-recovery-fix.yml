name: Bootstrap Password Recovery Fix - TEMPORARY

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/bootstrap-password-recovery-fix.yml'

permissions:
  contents: write

jobs:
  apply-fix:
    if: github.event.pull_request.head.ref == 'fix/password-recovery-compat-20260818' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Apply narrow password-recovery compatibility fix and regression suite
        shell: bash
        run: |
          set -euo pipefail
          python3 <<'PY'
          from pathlib import Path

          src_path = Path('supabaseClient.js')
          src = src_path.read_text()

          listener_anchor = """                window.supabaseClient.auth.onAuthStateChange(function (event, session) {
                    window.currentSupabaseSession = session;
                    window.currentSupabaseUser = session ? session.user : null;

                    if (session && session.user && session.access_token) {
          """
          listener_replacement = """                window.supabaseClient.auth.onAuthStateChange(function (event, session) {
                    window.currentSupabaseSession = session;
                    window.currentSupabaseUser = session ? session.user : null;

                    if (event === 'PASSWORD_RECOVERY') {
                        setPasswordRecoveryActive(true);
                        showPasswordRecoveryDialog();
                    }

                    if (session && session.user && session.access_token) {
          """
          if src.count(listener_anchor) != 1:
              raise SystemExit('Unexpected auth-state listener anchor count')
          src = src.replace(listener_anchor, listener_replacement, 1)

          hydrate_anchor = """                    window.currentSupabaseSession = sessionResp.data.session;
                    window.currentSupabaseUser = sessionResp.data.session.user;
                    updateAuthUIState(window.currentSupabaseUser);
                } else if (window.location.hash && window.location.hash.includes('access_token')) {
          """
          hydrate_replacement = """                    window.currentSupabaseSession = sessionResp.data.session;
                    window.currentSupabaseUser = sessionResp.data.session.user;
                    updateAuthUIState(window.currentSupabaseUser);
                    if (passwordRecoveryActive || hasPasswordRecoveryUrlMarker()) {
                        setPasswordRecoveryActive(true);
                        showPasswordRecoveryDialog();
                    }
                } else if (window.location.hash && window.location.hash.includes('access_token')) {
          """
          if src.count(hydrate_anchor) != 1:
              raise SystemExit('Unexpected session hydration anchor count')
          src = src.replace(hydrate_anchor, hydrate_replacement, 1)

          email_anchor = """    var emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;
          """
          helpers = r'''    var passwordRecoveryActive = safeGet('wingman_password_recovery_active') === 'true';
    var recoveryBodyOverflow = null;

    function authErrorCode(error) {
        return error && error.code ? String(error.code) : '';
    }

    function authErrorReasons(error) {
        return (error && Array.isArray(error.reasons)) ? error.reasons.map(function (reason) {
            return String(reason || '').toLowerCase();
        }) : [];
    }

    function formatAuthError(error, fallbackMessage) {
        var code = authErrorCode(error).toLowerCase();
        var name = error && error.name ? String(error.name).toLowerCase() : '';
        var reasons = authErrorReasons(error);
        var isWeakPassword = code === 'weak_password' || name.indexOf('weakpassword') !== -1 || name.indexOf('weak_password') !== -1;

        if (isWeakPassword) {
            if (reasons.indexOf('leaked_password') !== -1 || reasons.indexOf('pwned') !== -1) {
                return 'This password has appeared in known data breaches. Choose a different password that you do not reuse elsewhere.';
            }
            return 'This password does not meet the current security requirements. Choose a stronger password and try again.';
        }

        if (error && error.message) return String(error.message);
        return fallbackMessage || 'Authentication request failed.';
    }

    function hasPasswordRecoveryUrlMarker() {
        try {
            var search = window.location && window.location.search ? String(window.location.search) : '';
            if (typeof URLSearchParams !== 'undefined') {
                var params = new URLSearchParams(search);
                if (params.get('type') === 'recovery') return true;
            } else if (/(?:^|[?&])type=recovery(?:&|$)/.test(search)) {
                return true;
            }
            var hash = window.location && window.location.hash ? String(window.location.hash) : '';
            return /(?:^|[&#])type=recovery(?:&|$)/.test(hash);
        } catch (e) {
            return false;
        }
    }

    function setPasswordRecoveryActive(active) {
        passwordRecoveryActive = active === true;
        if (passwordRecoveryActive) safeSet('wingman_password_recovery_active', 'true');
        else safeRemove('wingman_password_recovery_active');
    }

    function cleanPasswordRecoveryUrl() {
        try {
            var pathname = (window.location && window.location.pathname) ? String(window.location.pathname) : '/app.html';
            var search = (window.location && window.location.search) ? String(window.location.search) : '';
            var cleanSearch = '';
            if (typeof URLSearchParams !== 'undefined') {
                var params = new URLSearchParams(search);
                params.delete('type');
                var encoded = params.toString();
                cleanSearch = encoded ? ('?' + encoded) : '';
            } else {
                cleanSearch = search.replace(/([?&])type=recovery(&|$)/, function (_, lead, tail) {
                    if (lead === '?' && tail === '&') return '?';
                    if (lead === '&' && tail === '&') return '&';
                    return '';
                }).replace(/[?&]$/, '');
            }
            if (window.history && typeof window.history.replaceState === 'function') {
                window.history.replaceState({}, document.title, pathname + cleanSearch);
            }
        } catch (e) {}
    }

    function removePasswordRecoveryDialog() {
        try {
            var overlay = document.getElementById('wingmanPasswordRecoveryOverlay');
            if (overlay && typeof overlay.remove === 'function') overlay.remove();
            else if (overlay && overlay.parentNode) overlay.parentNode.removeChild(overlay);
        } catch (e) {}
        try {
            if (document.body && document.body.style && recoveryBodyOverflow !== null) {
                document.body.style.overflow = recoveryBodyOverflow;
            }
        } catch (e) {}
        recoveryBodyOverflow = null;
    }

    function createRecoveryElement(tag, attributes, text) {
        var el = document.createElement(tag);
        Object.keys(attributes || {}).forEach(function (key) {
            if (key === 'style') el.style.cssText = attributes[key];
            else if (key === 'className') el.className = attributes[key];
            else if (key === 'type') el.type = attributes[key];
            else if (key === 'id') el.id = attributes[key];
            else if (key === 'disabled') el.disabled = attributes[key];
            else el.setAttribute(key, attributes[key]);
        });
        if (text !== undefined && text !== null) el.textContent = text;
        return el;
    }

    function showPasswordRecoveryDialog() {
        if (!passwordRecoveryActive && !hasPasswordRecoveryUrlMarker()) return;
        if (!document || !document.body) {
            if (document && typeof document.addEventListener === 'function') {
                document.addEventListener('DOMContentLoaded', showPasswordRecoveryDialog, { once: true });
            }
            return;
        }
        if (document.getElementById('wingmanPasswordRecoveryOverlay')) return;

        setPasswordRecoveryActive(true);
        recoveryBodyOverflow = document.body.style ? (document.body.style.overflow || '') : '';
        if (document.body.style) document.body.style.overflow = 'hidden';

        var overlay = createRecoveryElement('div', {
            id: 'wingmanPasswordRecoveryOverlay',
            role: 'dialog',
            'aria-modal': 'true',
            'aria-labelledby': 'wingmanPasswordRecoveryTitle',
            'aria-describedby': 'wingmanPasswordRecoveryDescription',
            style: 'position:fixed;inset:0;z-index:2147483647;display:flex;align-items:center;justify-content:center;padding:20px;background:rgba(0,0,0,.92);backdrop-filter:blur(14px);font-family:Inter,system-ui,sans-serif;'
        });
        var panel = createRecoveryElement('div', {
            style: 'width:min(100%,430px);border:1px solid rgba(139,92,246,.55);border-radius:18px;padding:24px;background:#0b0b12;box-shadow:0 24px 80px rgba(0,0,0,.7);color:#fff;'
        });
        var title = createRecoveryElement('h2', { id: 'wingmanPasswordRecoveryTitle', style: 'margin:0 0 8px;font-size:22px;line-height:1.25;font-weight:800;' }, 'Choose a new password');
        var description = createRecoveryElement('p', { id: 'wingmanPasswordRecoveryDescription', style: 'margin:0 0 18px;color:#cbd5e1;font-size:14px;line-height:1.5;' }, 'Your recovery link is verified. Set a new password to finish securing your account.');
        var form = createRecoveryElement('form', { id: 'wingmanPasswordRecoveryForm', novalidate: 'novalidate' });
        var label1 = createRecoveryElement('label', { for: 'wingmanRecoveryNewPassword', style: 'display:block;margin:0 0 6px;color:#e2e8f0;font-size:13px;font-weight:700;' }, 'New password');
        var input1 = createRecoveryElement('input', {
            id: 'wingmanRecoveryNewPassword', type: 'password', autocomplete: 'new-password', minlength: '8', required: 'required',
            style: 'width:100%;box-sizing:border-box;margin:0 0 14px;padding:12px 14px;border:1px solid rgba(255,255,255,.18);border-radius:10px;background:#050508;color:#fff;font-size:16px;outline:none;'
        });
        var label2 = createRecoveryElement('label', { for: 'wingmanRecoveryConfirmPassword', style: 'display:block;margin:0 0 6px;color:#e2e8f0;font-size:13px;font-weight:700;' }, 'Confirm new password');
        var input2 = createRecoveryElement('input', {
            id: 'wingmanRecoveryConfirmPassword', type: 'password', autocomplete: 'new-password', minlength: '8', required: 'required',
            style: 'width:100%;box-sizing:border-box;margin:0 0 14px;padding:12px 14px;border:1px solid rgba(255,255,255,.18);border-radius:10px;background:#050508;color:#fff;font-size:16px;outline:none;'
        });
        var error = createRecoveryElement('div', { id: 'wingmanRecoveryError', role: 'alert', 'aria-live': 'assertive', style: 'display:none;margin:0 0 12px;padding:10px 12px;border:1px solid rgba(248,113,113,.4);border-radius:9px;background:rgba(127,29,29,.35);color:#fecaca;font-size:13px;line-height:1.4;' });
        var submit = createRecoveryElement('button', { id: 'wingmanRecoverySubmit', type: 'submit', style: 'width:100%;min-height:46px;border:0;border-radius:10px;background:#7c3aed;color:#fff;font-size:15px;font-weight:800;cursor:pointer;' }, 'Update password');

        form.appendChild(label1); form.appendChild(input1); form.appendChild(label2); form.appendChild(input2); form.appendChild(error); form.appendChild(submit);
        panel.appendChild(title); panel.appendChild(description); panel.appendChild(form); overlay.appendChild(panel); document.body.appendChild(overlay);

        form.addEventListener('submit', async function (event) {
            if (event && typeof event.preventDefault === 'function') event.preventDefault();
            error.style.display = 'none';
            error.textContent = '';
            submit.disabled = true;
            submit.textContent = 'Updating…';
            var result = await window.completePasswordRecovery(input1.value || '', input2.value || '');
            if (!result || !result.success) {
                error.textContent = (result && result.error) ? result.error : 'Unable to update password. Please try again.';
                error.style.display = 'block';
                submit.disabled = false;
                submit.textContent = 'Update password';
            }
        });

        try { input1.focus(); } catch (e) {}
    }

    window.completePasswordRecovery = async function (newPassword, confirmPassword) {
        var password = String(newPassword || '');
        var confirmation = String(confirmPassword || '');
        if (!passwordRecoveryActive && !hasPasswordRecoveryUrlMarker()) {
            return { success: false, error: 'No active password recovery request.' };
        }
        if (password.length < 8) {
            return { success: false, error: 'Password must be at least 8 characters long.' };
        }
        if (password !== confirmation) {
            return { success: false, error: 'Passwords do not match.' };
        }

        var client = await initSupabase();
        if (!client || !client.auth) {
            return { success: false, error: 'Authentication service is initializing. Please try again.' };
        }

        try {
            var sessionResp = await client.auth.getSession();
            var session = sessionResp && sessionResp.data ? sessionResp.data.session : null;
            if (!session || !session.user) {
                return { success: false, error: 'This recovery session is missing or expired. Request a new password reset link.' };
            }

            var resp = await client.auth.updateUser({ password: password });
            if (resp.error) {
                var message = formatAuthError(resp.error, 'Unable to update password.');
                return { success: false, error: message, code: authErrorCode(resp.error), reasons: authErrorReasons(resp.error) };
            }

            setPasswordRecoveryActive(false);
            cleanPasswordRecoveryUrl();
            removePasswordRecoveryDialog();
            notifyUser('Password updated successfully.', 'success');
            return { success: true, user: resp.data && resp.data.user ? resp.data.user : null };
        } catch (err) {
            return { success: false, error: formatAuthError(err, 'Unable to update password.'), code: authErrorCode(err), reasons: authErrorReasons(err) };
        }
    };

    var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
'''
          if src.count(email_anchor) != 1:
              raise SystemExit('Unexpected emailRegex anchor count')
          src = src.replace(email_anchor, helpers, 1)

          signup_old = """            if (resp.error) {
                console.warn('Supabase Signup Error:', resp.error.message);
                notifyUser(resp.error.message, 'warning');
                return { success: false, error: resp.error.message };
            }
          """
          signup_new = """            if (resp.error) {
                var signupMessage = formatAuthError(resp.error, 'Sign up failed.');
                console.warn('Supabase Signup Error:', authErrorCode(resp.error) || resp.error.message);
                notifyUser(signupMessage, 'warning');
                return { success: false, error: signupMessage, code: authErrorCode(resp.error), reasons: authErrorReasons(resp.error) };
            }
          """
          if src.count(signup_old) != 1:
              raise SystemExit('Unexpected signup error block count')
          src = src.replace(signup_old, signup_new, 1)

          login_old = """            if (resp.error) {
                console.warn('Supabase Login Error:', resp.error.message);
                notifyUser(resp.error.message, 'warning');
                return { success: false, error: resp.error.message };
            }
          """
          login_new = """            if (resp.error) {
                var loginMessage = formatAuthError(resp.error, 'Login failed.');
                console.warn('Supabase Login Error:', authErrorCode(resp.error) || resp.error.message);
                notifyUser(loginMessage, 'warning');
                return { success: false, error: loginMessage, code: authErrorCode(resp.error), reasons: authErrorReasons(resp.error) };
            }
          """
          if src.count(login_old) != 1:
              raise SystemExit('Unexpected login error block count')
          src = src.replace(login_old, login_new, 1)

          src_path.write_text(src)

          test_path = Path('tests/password_recovery_compat.test.js')
          test_path.write_text(r'''/**
 * Password recovery completion + strengthened-password compatibility guard.
 * Executes the real supabaseClient.js handlers against an isolated Supabase/DOM runtime.
 */
const assert = require('assert');
const fs = require('fs');
const path = require('path');
const vm = require('vm');

const source = fs.readFileSync(path.join(__dirname, '../supabaseClient.js'), 'utf8');
assert(source.includes("event === 'PASSWORD_RECOVERY'"), 'Supabase PASSWORD_RECOVERY must be handled explicitly');
assert(source.includes('client.auth.updateUser({ password: password })'), 'Recovery must finish with auth.updateUser({password})');
assert(source.includes("code === 'weak_password'"), 'Weak-password handling must branch on structured Auth code');

class ClassList {
  constructor() { this.values = new Set(); }
  add(...v) { v.forEach(x => this.values.add(x)); }
  remove(...v) { v.forEach(x => this.values.delete(x)); }
  contains(v) { return this.values.has(v); }
}

function createEnvironment(options = {}) {
  const elements = new Map();
  const listeners = new Map();
  const storage = new Map();
  const calls = { signUp: [], signIn: [], reset: [], update: [], toasts: [], history: [] };
  let authCallback = null;
  let updateError = options.updateError || null;
  let session = options.session === undefined ? null : options.session;

  class Element {
    constructor(tag) {
      this.tagName = String(tag || 'div').toUpperCase();
      this.children = [];
      this.parentNode = null;
      this.classList = new ClassList();
      this.style = { cssText: '', overflow: '', display: '' };
      this.attributes = {};
      this.textContent = '';
      this.value = '';
      this.disabled = false;
      this.className = '';
      this._id = '';
      this._listeners = new Map();
    }
    set id(value) { this._id = String(value || ''); if (this._id) elements.set(this._id, this); }
    get id() { return this._id; }
    setAttribute(name, value) { this.attributes[name] = String(value); }
    appendChild(child) { child.parentNode = this; this.children.push(child); return child; }
    removeChild(child) { this.children = this.children.filter(x => x !== child); child.parentNode = null; if (child.id) elements.delete(child.id); return child; }
    remove() { if (this.parentNode) this.parentNode.removeChild(this); else if (this.id) elements.delete(this.id); }
    addEventListener(type, fn) { if (!this._listeners.has(type)) this._listeners.set(type, []); this._listeners.get(type).push(fn); }
    focus() {}
  }

  const body = new Element('body');
  const document = {
    body,
    title: 'MyWingman',
    readyState: 'loading',
    createElement: tag => new Element(tag),
    createTextNode: text => ({ nodeType: 3, textContent: String(text) }),
    getElementById: id => elements.get(id) || null,
    addEventListener(type, fn) { if (!listeners.has(type)) listeners.set(type, []); listeners.get(type).push(fn); }
  };

  const localStorage = {
    getItem: key => storage.has(key) ? storage.get(key) : null,
    setItem: (key, value) => storage.set(key, String(value)),
    removeItem: key => storage.delete(key),
    clear: () => storage.clear()
  };

  const client = {
    auth: {
      onAuthStateChange(cb) { authCallback = cb; return { data: { subscription: { unsubscribe() {} } } }; },
      getSession: async () => ({ data: { session }, error: null }),
      signUp: async payload => { calls.signUp.push(payload); return options.signUpResponse || { data: { user: null, session: null }, error: null }; },
      signInWithPassword: async payload => { calls.signIn.push(payload); return options.signInResponse || { data: { user: null, session: null }, error: null }; },
      signInWithOAuth: async () => ({ data: {}, error: null }),
      resetPasswordForEmail: async (email, cfg) => { calls.reset.push({ email, cfg }); return { data: {}, error: null }; },
      updateUser: async payload => { calls.update.push(payload); return updateError ? { data: null, error: updateError } : { data: { user: session && session.user ? session.user : { id: 'recovered-user' } }, error: null }; },
      signOut: async () => ({ error: null })
    },
    from: () => ({ select: () => ({ eq: () => ({ maybeSingle: async () => ({ data: null, error: null }) }) }) })
  };

  const location = {
    origin: 'https://mywingman.pages.dev',
    protocol: 'https:',
    hostname: 'mywingman.pages.dev',
    pathname: '/app.html',
    search: options.search || '',
    hash: options.hash || ''
  };
  const history = {
    replaceState(state, title, url) {
      calls.history.push(url);
      const q = String(url).indexOf('?');
      location.pathname = q === -1 ? String(url) : String(url).slice(0, q);
      location.search = q === -1 ? '' : String(url).slice(q);
      location.hash = '';
    }
  };

  const window = {
    location,
    history,
    supabase: { createClient: () => client },
    showToast: (msg, type) => calls.toasts.push({ msg, type }),
    __memoryStore: {}
  };
  window.window = window;
  window.document = document;
  window.localStorage = localStorage;
  window.sessionStorage = localStorage;

  const sandbox = {
    window,
    document,
    localStorage,
    sessionStorage: localStorage,
    URLSearchParams,
    console,
    setTimeout: fn => { fn(); return 1; },
    clearTimeout: () => {},
    Promise,
    Array,
    Object,
    String,
    Number,
    Boolean,
    RegExp,
    Date,
    Error,
    Math,
    JSON
  };
  vm.createContext(sandbox);
  vm.runInContext(source, sandbox);

  return {
    window,
    document,
    calls,
    storage,
    client,
    setUpdateError(value) { updateError = value; },
    setSession(value) { session = value; },
    getAuthCallback() { return authCallback; }
  };
}

(async () => {
  console.log('Running password recovery compatibility tests...');

  const weak = { code: 'weak_password', name: 'AuthWeakPasswordError', message: 'Password is weak', reasons: ['leaked_password'] };
  const signupEnv = createEnvironment({ signUpResponse: { data: { user: null, session: null }, error: weak } });
  const signup = await signupEnv.window.signUpUser('test@example.com', 'Password123!');
  assert.strictEqual(signup.success, false);
  assert.strictEqual(signup.code, 'weak_password');
  assert(signup.error.includes('known data breaches'));
  assert.strictEqual(signupEnv.storage.get('wingman_authenticated'), undefined);
  console.log('✓ weak/leaked signup is rejected using structured Auth error data');

  const loginEnv = createEnvironment({ signInResponse: { data: { user: null, session: null }, error: weak } });
  const login = await loginEnv.window.loginUser('test@example.com', 'Password123!');
  assert.strictEqual(login.success, false);
  assert.strictEqual(login.code, 'weak_password');
  assert(login.error.includes('known data breaches'));
  assert.strictEqual(loginEnv.storage.get('wingman_authenticated'), undefined);
  console.log('✓ weak-password sign-in response cannot falsely authenticate the UI');

  const resetEnv = createEnvironment();
  const reset = await resetEnv.window.resetPasswordForEmail('test@example.com');
  assert.strictEqual(reset.success, true);
  assert.strictEqual(resetEnv.calls.reset.length, 1);
  assert.strictEqual(resetEnv.calls.reset[0].cfg.redirectTo, 'https://mywingman.pages.dev/app.html?type=recovery');
  console.log('✓ reset email keeps the exact Cloudflare recovery redirect');

  const recoverySession = { user: { id: 'recovered-user', email: 'test@example.com' }, access_token: 'recovery-access-token' };
  const recoveryEnv = createEnvironment({ session: recoverySession });
  await recoveryEnv.window.resetPasswordForEmail('test@example.com'); // initializes real client/listener only
  const cb = recoveryEnv.getAuthCallback();
  assert.strictEqual(typeof cb, 'function');
  cb('PASSWORD_RECOVERY', recoverySession);
  assert(recoveryEnv.document.getElementById('wingmanPasswordRecoveryOverlay'), 'Recovery event must render the locked completion dialog');
  assert.strictEqual(recoveryEnv.storage.get('wingman_password_recovery_active'), 'true');

  const mismatch = await recoveryEnv.window.completePasswordRecovery('StrongPassword123!', 'DifferentPassword123!');
  assert.strictEqual(mismatch.success, false);
  assert.strictEqual(mismatch.error, 'Passwords do not match.');
  assert.strictEqual(recoveryEnv.calls.update.length, 0, 'Mismatch must never call updateUser');
  console.log('✓ confirmation mismatch is rejected before any password mutation');

  recoveryEnv.setUpdateError(weak);
  const weakUpdate = await recoveryEnv.window.completePasswordRecovery('StrongPassword123!', 'StrongPassword123!');
  assert.strictEqual(weakUpdate.success, false);
  assert.strictEqual(weakUpdate.code, 'weak_password');
  assert(weakUpdate.error.includes('known data breaches'));
  assert(recoveryEnv.document.getElementById('wingmanPasswordRecoveryOverlay'), 'Weak password error must keep recovery dialog active');
  assert.strictEqual(recoveryEnv.storage.get('wingman_password_recovery_active'), 'true');
  console.log('✓ leaked-password rejection keeps recovery completion safely active');

  recoveryEnv.setUpdateError(null);
  const success = await recoveryEnv.window.completePasswordRecovery('UniqueStrongPassword123!', 'UniqueStrongPassword123!');
  assert.strictEqual(success.success, true);
  assert.deepStrictEqual(JSON.parse(JSON.stringify(recoveryEnv.calls.update[1])), { password: 'UniqueStrongPassword123!' });
  assert.strictEqual(recoveryEnv.document.getElementById('wingmanPasswordRecoveryOverlay'), null);
  assert.strictEqual(recoveryEnv.storage.has('wingman_password_recovery_active'), false);
  assert.strictEqual(recoveryEnv.calls.history[recoveryEnv.calls.history.length - 1], '/app.html');
  console.log('✓ verified recovery session updates password exactly once and cleans recovery state');

  const ordinaryEnv = createEnvironment({ session: recoverySession });
  await ordinaryEnv.window.resetPasswordForEmail('test@example.com');
  ordinaryEnv.getAuthCallback()('SIGNED_IN', recoverySession);
  assert.strictEqual(ordinaryEnv.document.getElementById('wingmanPasswordRecoveryOverlay'), null, 'Ordinary sign-in must never open recovery UI');
  console.log('✓ ordinary authenticated sessions are unchanged');

  const fallbackEnv = createEnvironment({ session: recoverySession, search: '?type=recovery' });
  await fallbackEnv.window.resetPasswordForEmail('test@example.com');
  assert(fallbackEnv.document.getElementById('wingmanPasswordRecoveryOverlay'), 'Hydrated recovery URL + real session must restore completion UI');
  console.log('✓ recovery URL/session hydration survives callback timing differences');

  console.log('PASSWORD RECOVERY COMPATIBILITY: ALL TESTS PASSED');
})().catch(err => {
  console.error(err);
  process.exit(1);
});
''')

          runner = Path('tests/run_all_tests.js')
          text = runner.read_text()
          suite53 = "    { name: '53. Landing Explicit Favicon Delivery Guard', file: 'landing_favicon_delivery.test.js' }\n"
          suite54 = "    { name: '53. Landing Explicit Favicon Delivery Guard', file: 'landing_favicon_delivery.test.js' },\n    { name: '54. Password Recovery Completion & Strengthened-Password Guard', file: 'password_recovery_compat.test.js' }\n"
          if text.count(suite53) != 1:
              raise SystemExit('Unexpected suite #53 anchor count')
          runner.write_text(text.replace(suite53, suite54, 1))
          PY

          node tests/password_recovery_compat.test.js
          node -c supabaseClient.js
          node -c tests/password_recovery_compat.test.js
          grep -q "54. Password Recovery Completion" tests/run_all_tests.js

      - name: Remove bootstrap workflow and commit only production fix/tests
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/bootstrap-password-recovery-fix.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add supabaseClient.js tests/password_recovery_compat.test.js tests/run_all_tests.js .github/workflows/bootstrap-password-recovery-fix.yml
          git diff --cached --check
          git commit -m 'Complete Supabase password recovery flow'
          git push origin HEAD:fix/password-recovery-compat-20260818
